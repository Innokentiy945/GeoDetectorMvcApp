@model Microsoft.EntityFrameworkCore.DbSet<WeatherServiceWebApi.Model.WeatherModel>

<h2>List of weather data</h2>

<p><a asp-action="WeatherSet">Add Weather Data</a></p>
<table class="table">
    <tr>
        <th>CloudType</th>
        <th>RainPersantage</th>
        <th>SunPersantage</th>
        <th>Temperature</th>
        <th>GeoLocation</th>
    </tr>


    @foreach (var item in Model)
    {
        <tr>
            <td>@item.CloudType</td>
            <td>@item.RainPersantage</td>
            <td>@item.SunPersantage</td>
            <td>@item.Temperature</td>
            <td>@item.GeoLocation</td>
        </tr>
    }
</table>
